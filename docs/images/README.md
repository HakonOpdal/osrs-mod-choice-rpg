# README images

Capture these four shots (see the shot list in the root `README.md`) and save
them here with these exact filenames so the README table renders:

- `draft-cards.png` — side panel with a pending draft (three offers + reroll).
- `locked-region.png` — locked-region banner + shaded ground tiles.
- `side-panel.png` — idle side panel (points, threshold, counters, history).
- `unlock-message.png` — the "Pathlocked: Unlocked: …!" chat game message.

Optional: `loop.gif` — one full earn → threshold → draft → unlock loop.
Keep images reasonably sized (PNG, ideally < 500 KB each).
